[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
# Comece com erros objetivos que podem quebrar a execução. O conjunto pode ser
# ampliado gradualmente sem bloquear o projeto pelo passivo de estilo existente.
select = ["E9", "F63", "F7", "F82"]
